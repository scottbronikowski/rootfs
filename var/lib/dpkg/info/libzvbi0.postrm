#!/bin/sh -e

if [ "$1" = "purge" ] && test -e /etc/default/libzvbi0; then
	rm /etc/default/libzvbi0
fi

# Automatically added by dh_makeshlibs
if [ "$1" = "remove" ]; then
	ldconfig
fi
# End automatically added section

