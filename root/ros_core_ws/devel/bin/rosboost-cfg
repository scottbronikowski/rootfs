#!/usr/bin/env python
# creates a relay to a python script source file, acting as that file.
# The purpose is that of a symlink
with open("/root/ros_core_ws/src/ros/tools/rosboost_cfg/scripts/rosboost-cfg", 'r') as fh:
    exec(fh.read())
