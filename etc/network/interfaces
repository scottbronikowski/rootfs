# interfaces(5) file used by ifup(8) and ifdown(8)

#auto eth0
#iface eth0 inet dhcp 

#loopback interface
auto lo
iface lo inet loopback

#eth0 for USB 4G modem
allow-hotplug eth0
#auto eth0
iface eth0 inet dhcp

#wifi

### USB Wifi adapter
allow-hotplug wlan1
auto wlan1
## NEW -- wpa-roam
#iface wlan1 inet manual
#     	#pre-up sh -c "sleep 5"
#	wpa-driver wext
#      	wpa-roam /etc/wpa_supplicant.conf 
#	# wpa-conf /etc/wpa_supplicant.conf
#iface default inet dhcp
#iface pal3 inet dhcp

# OLD
 iface wlan1 inet dhcp
#	wpa-driver wext
#	wpa-conf /etc/wpa_supplicant.conf
#iface pal3 inet dhcp
#      pre-up wpa_supplicant -Dwext -iwlan1 -c/etc/wpa_supplicant.conf -B
       pre-up wpa_supplicant -iwlan1 -c/etc/wpa_supplicant.conf -B		
       down killall wpa_supplicant
##	pre-up /sbin/iwconfig wlan0 essid qobi-cccp
#	wireless-essid qobi-office
#	wireless-mode managed

### built-in wifi adapter
allow-hotplug wlan0
#auto wlan0
#iface wlan0 inet dhcp
##      pre-up wpa_supplicant -Dwext -iwlan0 -c/etc/wpa_supplicant.conf -B
#	pre-up wpa_supplicant -iwlan0 -c/etc/wpa_supplicant.conf -B		
#      	down killall wpa_supplicant
##	pre-up /sbin/iwconfig wlan0 essid qobi-cccp
##	wireless-essid qobi-office
##	wireless-mode managed


#allow-hotplug wlan2
#auto wlan2
#iface wlan2 inet dhcp
##      pre-up wpa_supplicant -Dwext -iwlan2 -c/etc/wpa_supplicant.conf -B
##      down killall wpa_supplicant
##	pre-up /sbin/iwconfig wlan0 essid qobi-cccp
#	wireless-essid qobi-cccp
#	wireless-mode managed
